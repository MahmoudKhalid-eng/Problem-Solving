#include <bits/stdc++.h>
using namespace std;
void fast()
{
    ios_base :: sync_with_stdio( 0 ) ;
    cin.tie( 0 ) ;
    cout.tie( 0 ) ;
}
int main(){
	fast();
	/// # of digits
	/*
	int n;
	cin>>n;
	cout<<1+floor(log10(n));
	*/
	/// print all numbers of n digits
	/*
	int n;
	cin>>n;
	for(int i=pow(10,n-1);i<=pow(10,n)-1;i++){
        cout<<i<<"\n";
	}
    */

	return 0;
}
